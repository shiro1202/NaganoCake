<div class="container">
  <div class="row">
    <div class="col">

    <h2>ジャンル編集</h2>

    <%= form_with model: @genre, url: admin_genre_path(@genre), method: :patch, local:true do |f| %>
      <h4>ジャンル名</h4>
      <%= f.text_field :name %>
      <%= f.submit '変更を保存' %>
    <% end %>

    </div>
  </div>
</div>